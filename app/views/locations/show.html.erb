<% content_for :title, @location.name %>
<% content_for :meta_description, (@location.description.presence || "News and coverage from #{@location.name} on Joetsu-Myoko Daily.") %>
<% lead = @articles.first; if lead&.featured_image&.attached? %>
  <% content_for :og_image, url_for(lead.featured_image) %>
<% end %>

<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
  <%= render "shared/index_header",
      title: @location.name,
      description: @location.description,
      count: @pagy.count,
      breadcrumbs: [
        { name: t("nav.home", default: "Home"), url: locale_root_path },
        { name: @location.name, url: request.path }
      ] %>

  <%= render "shared/paginated_article_list",
      articles: @articles,
      pagy: @pagy,
      empty_heading: "No articles for this location yet.",
      empty_body: "Check back soon — we're always adding new coverage." %>
</div>
